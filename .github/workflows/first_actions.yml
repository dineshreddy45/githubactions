name:  My first actions
on:  push
jobs:
  build:
    runs-on:  ubuntu-latest
    steps:
    - name: Checkout
      uses:  actions/Checkout@v4
    - name: Build 
      run: "echo building"
    
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: echo "testing"
  
  deploy:
    needs: test
    runs-on:  ubuntu-latest
    steps:
    - name: Deploy
      run:  echo "Deploying"